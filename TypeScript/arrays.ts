const superHeroes: string[] = []
const heroNumber: number[] = []
// can also be defined as
const heroNumber2: Array<number> = []

type User = {
  name: string
  isActive: boolean
}

const allUsers: User[] = []

// Multidimensional
const MLModels: number[][] = [[255, 255, 255]]

superHeroes.push("spiderman")
heroNumber.push(2)

allUsers.push({
  name: "Deveesh",
  isActive: false,
})

export {}
